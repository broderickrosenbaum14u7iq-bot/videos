{
  "name": "r2-media-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker that validates HMAC-signed playback URLs and streams video from a private R2 bucket.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test/**/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
